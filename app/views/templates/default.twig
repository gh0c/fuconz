<!DOCTYPE html>
<html lang="en">
<head>
    {% include 'templates/partials/head_info.twig'%}
    <title>{% block title %} {% endblock %}</title>
</head>

<body>

    <div class = "a-level-holder">

        <!-- sidebar for logged users -->
        {%  if auth_user.show_sidebar %}
            {% include 'user/user.sidebar.twig' %}
        {% endif %}

        <div class = "content-wrap">
            <div class = "content">
                {% include 'templates/std_header.twig'%}

                <section id = "content">
                    <!-- main content, will be injected via Twig -->
                    {% block content %} {% endblock %}
                </section>

            </div>
        </div>

        <div class="push"></div>


    </div>

    {% include 'templates/std_footer.twig'%}


{% include 'templates/partials/foot_info.twig'%}

{% block additional_js %}
    {% if auth_user.show_sidebar %}
        <script src="public/js/SideslideSidebar/classie.js"></script>
        <script src="public/js/SideslideSidebar/main.js"></script>
    {% endif %}
{% endblock %}
</body>

</html>